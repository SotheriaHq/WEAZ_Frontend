import React from 'react';

// Legacy placeholder: this page has been replaced by the unified BrandProfile view.
// Kept as a no-op to avoid tsconfig include errors until the file can be safely removed.
const BrandPublic: React.FC = () => null;
export default BrandPublic;

