// React import not required with the new JSX transform

const Trouble = () => {
  return (
    <div>
      <h1 className="title-text">Troubleshooting</h1>
    </div>
  );
};

export default Trouble;
